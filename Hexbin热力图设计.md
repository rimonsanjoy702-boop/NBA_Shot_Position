# Hexbin 全场对比热力图设计

## 1. 布局

一张完整的篮球场俯视图（横向），左侧篮筐和右侧篮筐各带一个半场 Hexbin 热力图。两侧数据源独立选择，形成对比分析。

```
┌──────────────────────────────────────────────────────────────────┐
│  左侧面板                              │  右侧面板                 │
│  [球员/球队 ▼] [赛季 ▼]               │  [球员/球队 ▼] [赛季 ▼]   │
│                                        │                          │
│   ┌─────────────┐         ┌─────────────┐                        │
│   │   ◯ 篮筐    │         │    篮筐 ◯   │                        │
│   │    \       │         │        /    │                        │
│   │      \     │         │      /      │                        │
│   │  左半场     │  中场   │   右半场     │                        │
│   │  Hexbin    │         │   Hexbin    │                        │
│   │            │         │             │                        │
│   └─────────────┘         └─────────────┘                        │
│                                        │                          │
│               [☑ 固定颜色标尺]  [▶ 逐年播放]                       │
└──────────────────────────────────────────────────────────────────┘
```

数据集的原始坐标以进攻方篮筐为原点。全场图将左篮筐作为「左半场」的进攻篮筐，右篮筐作为「右半场」的进攻篮筐。比分牌放在两半场之间。

---

## 2. 视觉编码

| 维度 | 编码方式 | 说明 |
|---|---|---|
| 投篮量 | 格子大小 | 出手越多，六边形越大 |
| 命中率 | 颜色深浅 | 统一橙红色系，深 = 命中率高，浅 = 低 |
| 颜色标尺 | 两半场共用 | 默认自动适配双方数据范围，可选固定 0~100% |

> 一张图同时回答两个问题：**"在哪里投"**（大小）和 **"在哪里准"**（深浅）。

---

## 3. 数据粒度

### 三级选择

| 层级 | 范围 | 选择方式 |
|---|---|---|
| 全联盟 | 全部 4.7M 投篮 | 快捷按钮「联盟平均」 |
| 球队 | 全部 30 支（按 Team ID） | 下拉选择器 |
| 球员 | 12 位预设典型球星 | 下拉选择器 |

- 球员和球队二选一：选了球员就不能再选球队，反之亦然。
- 不选时默认「联盟平均」。

### 12 位预设球员

| 球员 | 叙事价值 |
|---|---|
| Stephen Curry | 三分革命符号 |
| LeBron James | 跨时代巨星 |
| Michael Jordan | 90 年代统治者 |
| Kobe Bryant | 中距离大师 |
| James Harden | 魔球极端代表 |
| Kevin Durant | 中距离 + 三分全能 |
| Klay Thompson | 接球就投射手范本 |
| Damian Lillard | 超远三分先行者 |
| Shaquille O'Neal | 禁区统治者 |
| Dwight Howard | 传统中锋对照 |
| Kawhi Leonard | 中距离效率 + 稳定性 |
| Brook Lopez | 禁区 → 空间型中锋转型 |

---

## 4. 时间控制

每个半场独立选择赛季（共 23 个赛季标签），支持三种对比模式：

| 模式 | 左半场 | 右半场 | 示例 |
|---|---|---|---|
| 同赛季对比 | 2018-19, Curry | 2018-19, Harden | "谁投篮范围更广？" |
| 跨赛季对比 | 2012-13, Curry | 2018-19, Curry | "Curry 进化了多少？" |
| 跨时代论战 | 1997-98, Jordan | 2017-18, LeBron | "两个时代王者差异" |

附加功能：**逐年播放按钮**，按 1 秒/赛季自动推进，动画展示三分革命的空间演变。

---

## 5. 坐标系与半场映射

### 原始坐标

```
原点 (0, 0) = 进攻方篮筐正下方
X: -250 (左) ~ +250 (右)，球场宽度方向
Y: 0 ~ 470 (从篮筐向外沿纵轴到半场线附近)
```

- 所有投篮记录的坐标都以**进攻方篮筐**为原点
- 两个半场各自渲染时，都以自己的篮筐为原点，无需坐标翻转
- SVG 底图做一个完整的横向全场（两个篮筐 + 球场线），两个 D3 hexbin 组件各自定位到对应的半场位置

### 全场映射

原始数据中所有投篮属于进攻方前场。全场图需要将数据分配到两个半场：

```
    进攻篮筐 (Y=0)
    │  ↑ 半场1 = 球队A前场 (Y 0~470)
    │
    ────── 中场 (Y≈470) ──────
    │
    │  ↓ 半场2 = 球队B前场 (Y 470~940)
    防守篮筐 (Y=940 = 镜像原点)
```

### 右半场映射

数据集的所有投篮都以进攻方篮筐为原点。要画全场横向图，右半场的数据需要映射到右侧篮筐：

```
        左篮筐 (Y=0)                             右篮筐 (Y=0')
        │                                        │
        │  ← 左半场 Y→   │  中场  │  ← Y' 右半场  │
        │                │        │              │
        (470)            │        │           (470')
```

将右半场数据映射到右侧篮筐为原点的坐标系：

```python
# 右半场：翻转 Y 轴，使投篮从右侧篮筐向外展开
Y_display = Y_original      # 从右篮筐向后场方向延伸
# 对于全场图，右半场不需要翻转——数据本身就记录进攻方前场
# 只需在渲染时让右半场的 D3 组件将原点放在右侧篮筐位置
```

> 简化方案：两个半场各用原始坐标渲染，各自的 SVG 组件以各自篮筐为原点。前端不翻转坐标，仅调整 SVG transform 平移。

---

## 6. JSON 数据集规范

### 文件组织

```
public/data/
  hexbin_season_1997-98.json
  hexbin_season_1998-99.json
  ...
  hexbin_season_2019-20.json   # 共 23 个文件
```

### 单文件结构

```json
{
  "season": "1997-98",
  "league": {
    "hexbins": [
      { "x": 50, "y": 30, "count": 1247, "fg_pct": 0.423 }
    ]
  },
  "teams": {
    "1610612744": {
      "team_name": "Golden State Warriors",
      "abbr": "GSW",
      "hexbins": [
        { "x": 30, "y": 200, "count": 85, "fg_pct": 0.481 }
      ]
    }
  },
  "players": {
    "76001": {
      "player_name": "Stephen Curry",
      "team_id": 1610612744,
      "team_abbr": "GSW",
      "hexbins": [
        { "x": -220, "y": 420, "count": 12, "fg_pct": 0.417 }
      ]
    }
  }
}
```

### 字段规范

| 字段 | 类型 | 约束 |
|---|---|---|
| `season` | string | 固定格式 `"YYYY-YY"` |
| `x` | integer | -250 ~ 250，hex 中心点 X 坐标 |
| `y` | integer | 0 ~ 470，hex 中心点 Y 坐标 |
| `count` | integer | ≥ 1，该六边形内投篮总次数 |
| `fg_pct` | number | 0.0 ~ 1.0，保留 3 位小数，命中次数 / 总次数 |
| `team_id` | integer | NBA 官方 Team ID |
| `team_name` / `abbr` | string | 全名和缩写，用于 UI 展示 |
| `player_name` | string | 全名，用于 UI 展示 |

### 解释

每个 JSON 对象代表一个六边形格子的聚合结果：
- `x`, `y` — hex 中心坐标（D3 hexbin 算法确定）
- `count` — 该六边形内的总出手次数
- `fg_pct` — 该六边形内的命中率 = 命中数 / 总出手数

前端使用方式：
- `count` → 决定六边形大小
- `fg_pct` → 决定颜色深浅（橙红色系）

---

## 7. 预处理参数

| 参数 | 值 | 说明 |
|---|---|---|
| Hex 半径 | 15 英尺 | 平衡粒度与总格子数 |
| 半场 Y 范围 | 0 ~ 470 | 过滤后场投篮 |
| X 范围 | -250 ~ 250 | 球场宽度边界 |
| 联盟/球队 min count | ≥ 5 | 过滤低频 hex |
| 球员 min count | ≥ 3 | 单球员出手基数较小 |
| 赛季标签格式 | `YYYY-YY` | 如 "1997-98" |
| 单个文件预估大小 | ~435 KB | 联盟 + 30 球队 + 12 球员 |
| 23 文件总计 | ~10 MB | — |

### 赛季定义

```python
# 从 Game Date (YYYYMMDD int) 推导赛季标签
year = game_date // 10000       # 如 19980102 → 1998
month = (game_date // 100) % 100  # → 01
if month >= 10:
    season = f"{year}-{str(year+1)[2:]}"    # "1997-98"
else:
    season = f"{year-1}-{str(year)[2:]}"    # "1997-98"
```

---

## 8. 前端消费方式

### 数据加载

1. 用户选择赛季 → Pinia Store 检查该赛季 JSON 是否已缓存
2. 未缓存 → `fetch(hexbin_season_{season}.json)` → 写入 Store
3. Store 根据选中层级（league / team / player）提取对应 hexbin 数组
4. 两个半场各有独立的 `seasonScope + scopeType + entityId`，各自提取数据
5. D3 组件监听数据变更，自动重绘

### 颜色标尺同步

两个半场默认使用**动态标尺**——基于左右两侧数据的并集计算 `fg_min` 和 `fg_max`，确保颜色含义一致。提供「固定标尺」开关，切换为 0.0 ~ 1.0 全局基准。

---

## 9. 核验清单

预处理脚本运行完毕后逐项确认：

- [ ] 23 个 JSON 文件全部生成，无空文件
- [ ] 每个文件中 `league.hexbins` 非空
- [ ] 所有 `fg_pct` ∈ [0, 1]
- [ ] 所有 `x` ∈ [-250, 250]，`y` ∈ [0, 470]
- [ ] 所有 `count` ≥ 对应最小阈值
- [ ] 12 位预设球员在有数据的赛季中均存在
- [ ] 总目录大小 ≤ 15 MB
- [ ] 前端 fetch 单个文件 < 200ms（本地环境）
- [ ] 两半场各以各自篮筐为原点渲染，无坐标越界
